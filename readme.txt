Report File:

The report is in a directory of its own called "Report"
as there is another PDF that needs to be looked at 
along with the report itself.

Website:

import dump.sql to localhost server
Use Xampp to access index via localhost
Follow prompts on Index page to login/register

